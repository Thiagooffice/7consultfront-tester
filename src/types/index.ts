export type SignInData = {
  email: string
  password: string
};

export type Client = {
  uuid: string
  name: string
  updated_at: string
  created_at: string
}

export type RegisterClient = {
  name: string
}